import React from 'react';
import SpecEntry from './SpecEntry';

const Specs = props => (
  <div className="specs">
    <SpecEntry />
    <SpecEntry />
    <SpecEntry />
    <SpecEntry />
  </div>
)

export default Specs;